% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_forest.docfixed.R
\name{ray_pipeline_forest}
\alias{ray_pipeline_forest}
\title{Run the forest RayExtract pipeline}
\usage{
ray_pipeline_forest(
  input_file,
  project_root = NULL,
  out_root = NULL,
  run_name = NULL,
  mount_dir = "/workspace/project",
  input_mode = NULL,
  raycloud_image = NULL,
  treetools_image = NULL,
  ray_direction = "0,0,-1",
  max_intensity = 0,
  remove_start_pos = FALSE,
  use_colour = FALSE,
  colour_mode = "alpha 1",
  args_import = "",
  existing_raycloud = NULL,
  reuse_existing_import = c("never", "fallback", "prefer"),
  search_sibling_raycloud = TRUE,
  args_colour = "",
  args_terrain = "",
  args_trees = "",
  args_split = "",
  args_smooth = "",
  args_info = "",
  args_mesh = "",
  steps = NULL,
  stop_after = NULL,
  resume = FALSE,
  overwrite = FALSE,
  dry_run = FALSE,
  echo = interactive()
)
}
\arguments{
\item{input_file}{Path to the starting input file for the chosen \code{input_mode}.}

\item{project_root}{Project root mounted into Docker.}

\item{out_root}{Optional run folder path.}

\item{run_name}{Optional run name used under \verb{<project_root>/runs/}.}

\item{mount_dir}{Docker mount path inside the container.}

\item{input_mode}{One of \code{"point_cloud"}, \code{"raycloud"}, \code{"treefile"},
or \code{"smoothed_treefile"}.}

\item{raycloud_image}{Docker image used for RayCloudTools commands.
Defaults to package options.}

\item{treetools_image}{Docker image used for TreeTools commands.
Defaults to the RayCloudTools image when unset.}

\item{ray_direction}{Ray direction string passed to \code{rayimport},
for example \code{"0,0,-1"}.}

\item{max_intensity}{Maximum intensity passed to \code{rayimport}.}

\item{remove_start_pos}{Logical; passed to \code{rayimport} when supported.}

\item{use_colour}{Logical; whether to run the colour step when relevant.}

\item{colour_mode}{Mode string passed to \code{raycolour}.}

\item{args_import}{Additional raw command-line arguments passed to \code{rayimport}.}

\item{existing_raycloud}{Optional explicit existing raycloud path.}

\item{reuse_existing_import}{Import reuse policy forwarded to \code{ray_import()}.}

\item{search_sibling_raycloud}{Logical; search for \verb{<stem>_raycloud.ply}
beside a raw input point cloud.}

\item{args_colour}{Additional raw command-line arguments passed to \code{raycolour}.}

\item{args_terrain}{Additional raw command-line arguments passed to \verb{rayextract terrain}.}

\item{args_trees}{Additional raw command-line arguments passed to \verb{rayextract trees}.}

\item{args_split}{Additional raw command-line arguments passed to \code{raysplit}.}

\item{args_smooth}{Additional raw command-line arguments passed to \code{treesmooth}.}

\item{args_info}{Additional raw command-line arguments passed to \code{treeinfo}.}

\item{args_mesh}{Additional raw command-line arguments passed to \code{treemesh}.}

\item{steps}{Optional character vector of step names to run.}

\item{stop_after}{Optional step name after which to stop.}

\item{resume}{Logical; reuse existing prerequisites when available.}

\item{overwrite}{Logical; rerun requested steps even if outputs exist.}

\item{dry_run}{Logical; construct commands without running Docker.}

\item{echo}{Logical; print Docker command output.}
}
\value{
A \code{ray_result} object summarizing the run.
}
\description{
Orchestrates the step functions in \code{rayforestr} with support for selective
steps, resume logic, and multiple entry modes.
}
\details{
Supported \code{input_mode} values are:
\itemize{
\item \code{"point_cloud"}: start from a raw \code{.ply} and optionally run \code{rayimport}
\item \code{"raycloud"}: start from an existing \verb{*_raycloud.ply}
\item \code{"treefile"}: start from an existing \verb{*_trees.txt}
\item \code{"smoothed_treefile"}: start from an existing \verb{*_smoothed.txt}
}

For \code{input_mode = "point_cloud"}, import behavior is controlled by
\code{reuse_existing_import}:
\itemize{
\item \code{"never"}: always try a fresh import
\item \code{"prefer"}: use an existing raycloud immediately
\item \code{"fallback"}: try fresh import first, then reuse an existing raycloud
}
}
