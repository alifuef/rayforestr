% Generated by roxygen2: do not edit by hand
% Please edit documentation in
%   R/rayforestr_import_docs_steps_import_colour_final.R
\name{ray_import}
\alias{ray_import}
\title{Import a point cloud into a RayCloudTools raycloud}
\usage{
ray_import(
  input_file,
  project_root,
  out_dir,
  ray_direction = "0,0,-1",
  max_intensity = 0,
  remove_start_pos = FALSE,
  args_import = "",
  existing_raycloud = NULL,
  reuse_existing = c("never", "fallback", "prefer"),
  search_sibling_raycloud = TRUE,
  docker_image = ray_default_raycloud_image(),
  mount_dir = "/workspace/project",
  dry_run = FALSE,
  echo = interactive(),
  log_file = NULL,
  skip_existing = FALSE
)
}
\arguments{
\item{input_file}{Path to the raw input point cloud (\code{.ply}).}

\item{project_root}{Project root mounted into Docker.}

\item{out_dir}{Stage output directory for the import step.}

\item{ray_direction}{Character vector like \code{"0,0,-1"} passed to \code{rayimport}.}

\item{max_intensity}{Numeric value forwarded to \code{--max_intensity}.}

\item{remove_start_pos}{Logical; include \code{--remove_start_pos} when \code{TRUE}.}

\item{args_import}{Additional raw command-line arguments for \code{rayimport}.}

\item{existing_raycloud}{Optional path to an existing \verb{*_raycloud.ply}.}

\item{reuse_existing}{One of \code{"never"}, \code{"fallback"}, or \code{"prefer"}.
\code{prefer} reuses an existing raycloud immediately. \code{fallback} tries raw
import first and reuses an existing raycloud only if needed.}

\item{search_sibling_raycloud}{Logical; search beside \code{input_file} for
\verb{<stem>_raycloud.ply} as a fallback candidate.}

\item{docker_image}{Docker image used for the import command.}

\item{mount_dir}{Mount point used inside the container.}

\item{dry_run}{Logical; construct commands without running Docker.}

\item{echo}{Logical; print Docker command output.}

\item{log_file}{Optional log file path.}

\item{skip_existing}{Logical; skip the step if the expected stage raycloud
already exists.}
}
\value{
A \code{ray_result} object.
}
\description{
Runs \code{rayimport} inside the stage folder and returns the resulting
\verb{*_raycloud.ply}. The function can also reuse an existing raycloud when
raw import is unreliable or when a validated raycloud already exists.
}
